struct Student {
    int age;
    string name;
}

fun int main() {
    var Student s;

    s.age = 18;
    s.name = "Ana";

    trace variables(s.age, s.name);

    print(s.name);
    print(s.age);

    return 0;
}
